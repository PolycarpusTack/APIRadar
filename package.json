{
  "name": "drift-monitor",
  "private": true,
  "scripts": {
    "dev:ui": "pnpm --filter drift-ui dev",
    "dev:desktop": "pnpm --filter drift-desktop dev",
    "build:ui": "pnpm --filter drift-ui build",
    "build:desktop": "pnpm --filter drift-desktop build",
    "lint": "pnpm --filter drift-ui lint && pnpm --filter drift-desktop lint"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["electron", "esbuild", "@esbuild/win32-x64"]
  }
}
